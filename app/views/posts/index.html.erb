<h1>Articles du blog : <%= @blog.name %></h1>

<%= link_to "Nouvel article", new_blog_post_path(@blog) %>

<ul>
  <% @posts.each do |post| %>
    <li>
      <strong><%= link_to post.title, blog_post_path(@blog, post) %></strong>
    </li>
  <% end %>
</ul>
